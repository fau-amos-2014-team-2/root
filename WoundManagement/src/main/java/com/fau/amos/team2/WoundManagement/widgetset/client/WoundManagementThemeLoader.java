package com.fau.amos.team2.WoundManagement.widgetset.client;

import com.vaadin.addon.touchkit.gwt.client.ThemeLoader;

public class WoundManagementThemeLoader extends ThemeLoader {

	@Override
	public void load() {
		// Load the default touchkit theme
		super.load();
		
		/* Resources.INSTANCE.baseCss().ensureInjected();
		Resources.INSTANCE.touchkitCss().ensureInjected();
		
		// Load my theme
		Resources.INSTANCE.css().ensureInjected(); */
	}
	
}
